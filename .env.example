VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Push notifications (049_push_notifications.sql). Public key only — safe to
# ship client-side. Generate a pair with: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=
